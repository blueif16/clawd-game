{
  "screenSize": {
    "width": {
      "value": 1280,
      "type": "number",
      "description": "Game design width in pixels. 16:9 so Scale.FIT is full-bleed (no letterbox bars) on a standard 16:9 monitor; the camera + physics world bounds equal this design resolution (single source of truth) so level content fills the frame."
    },
    "height": {
      "value": 720,
      "type": "number",
      "description": "Game design height in pixels. 16:9 (1280x720) — see width."
    }
  },
  "debugConfig": {
    "debug": {
      "value": false,
      "type": "boolean",
      "description": "Enable physics debug rendering"
    }
  },
  "renderConfig": {
    "pixelArt": {
      "value": true,
      "type": "boolean",
      "description": "Enable pixel-art (nearest-neighbour) rendering"
    }
  },
  "controlsHelp": [],
  "failModel": "health",
  "objective": ""
}
