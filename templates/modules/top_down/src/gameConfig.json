{
  "screenSize": {
    "width": {
      "value": 1280,
      "type": "number",
      "description": "VIEWPORT design width in pixels (the browser frame, NOT the world). 16:9 so Scale.FIT is full-bleed (no letterbox bars) on a standard 16:9 monitor. The WORLD a level occupies is the level scene's mapWidth/mapHeight, which may be LARGER than the viewport (a multi-screen scrolling level) — the camera follows the player so the viewport stays full at every scroll position; for a single-screen level mapWidth/mapHeight == this viewport size."
    },
    "height": {
      "value": 720,
      "type": "number",
      "description": "Game design height in pixels. 16:9 (1280x720) — see width."
    }
  },
  "debugConfig": {
    "debug": {
      "value": false,
      "type": "boolean",
      "description": "Enable physics debug rendering"
    }
  },
  "renderConfig": {
    "pixelArt": {
      "value": true,
      "type": "boolean",
      "description": "Enable pixel-art (nearest-neighbour) rendering"
    }
  },
  "playerConfig": {
    "maxHealth": {
      "value": 100,
      "type": "number",
      "description": "Player maximum health"
    },
    "walkSpeed": {
      "value": 200,
      "type": "number",
      "description": "Player 8-direction walk speed (px/s)"
    },
    "attackDamage": {
      "value": 25,
      "type": "number",
      "description": "Player melee/ranged attack damage"
    },
    "dashSpeed": {
      "value": 500,
      "type": "number",
      "description": "Dash speed (px/s)"
    },
    "dashDuration": {
      "value": 200,
      "type": "number",
      "description": "Dash duration (ms)"
    },
    "dashCooldown": {
      "value": 1000,
      "type": "number",
      "description": "Dash cooldown between uses (ms)"
    },
    "hurtingDuration": {
      "value": 100,
      "type": "number",
      "description": "Hurt-state duration in ms"
    },
    "invulnerableTime": {
      "value": 1000,
      "type": "number",
      "description": "Invulnerability window after taking damage (ms)"
    }
  },
  "enemyConfig": {
    "maxHealth": {
      "value": 50,
      "type": "number",
      "description": "Default enemy max health"
    },
    "walkSpeed": {
      "value": 80,
      "type": "number",
      "description": "Default enemy movement speed (px/s)"
    },
    "damage": {
      "value": 15,
      "type": "number",
      "description": "Default enemy contact damage"
    }
  },
  "controlsHelp": [],
  "failModel": "health",
  "objective": ""
}
